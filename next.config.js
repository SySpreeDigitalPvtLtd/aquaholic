/** @type {import('next').NextConfig} */
const path = require("path");

const nextConfig = {
  reactStrictMode: false,
  swcMinify: true,
  // images: {
  //   unoptimized: true,
  // },
};

module.exports = nextConfig;
